import React from 'react';

//child of ShoppingList
//allows user to add new items to shopping list
// AddItem component should accept a callback function as a prop, which is called when the user submits a new item. The callback function should update the items using the state variable in the ShoppingList component.
export default function AddItem({callback}) {
  return (
    <div>AddItem</div>
  )
}
